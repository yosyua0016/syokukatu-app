<div class="container">
  <div class="row justify-content-center">
    <div class="col-lg-6">
      <%= form_with model: @user, url: user_session_path, class: 'registration-main', local: true do |f| %>
        <div class='form-wrap' style="margin-top: 70px;"> <!-- 上に70pxの余白を追加 -->
          <div class='form-header'>
            <h1 class='form-header-text font-weight-bold' style="font-size: 2.5em;">会員情報入力</h1> <!-- フォントサイズをさらに大きくしてみました -->
          </div>
          <div class='login-flash-message'>
            <%= flash[:notice] %>
            <%= flash[:alert] %>
          </div>
          <div class="form-group" style="margin-top: 70px;"> <!-- 上に70pxの余白を追加 -->
            <div class='form-text-wrap'>
              <label class="form-text">メールアドレス</label>
              <span class="indispensable">必須</span>
            </div>
            <%= f.email_field :email, class:"input-default", id:"email", placeholder:"PC・携帯どちらでも可", autofocus: true %>
          </div>
          <div class="form-group" style="margin-top: 70px;"> <!-- 上に70pxの余白を追加 -->
            <div class='form-text-wrap'>
              <label class="form-text">パスワード</label>
              <span class="indispensable">必須</span>
            </div>
            <%= f.password_field :password, class:"input-default", id:"password", placeholder:"" %>
          </div>
          <div class='login-btn' style="margin-top: 70px;"> <!-- 上に70pxの余白を追加 -->
            <%= f.submit "ログイン" ,class:"login-red-btn" %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>